{
  "name": "codex-doudizhu",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.5" },
  "scripts": {
    "codex": "node scripts/launcher.mjs --open",
    "codex:inspect": "node scripts/launcher.mjs --no-open",
    "app:prepare": "node scripts/prepare-tauri-app.mjs",
    "app:dev": "npm run app:prepare && tauri dev",
    "app:build": "npm run app:prepare && CI=true tauri build --target universal-apple-darwin --bundles app,dmg",
    "check": "node --check scripts/launcher.mjs && node --check server/game-server.mjs && node --check inject/codex-doudizhu.user.js && node --check game/web-desktop/codex-entry.js"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}
